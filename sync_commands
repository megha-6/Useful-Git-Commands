## BELOW MENTIONED ARE THE GIT COMMANDS RELATED TO SYNC ##

# Command to fetch down all the branches from that Git remote.
git fetch [alias]

# Commadn to merge a remote branch into your current branch to bring it up to date.
git merge [alias]/[branch]

# Transmit local branch commits to the remote repository branch
git push [alias] [branch]

# Fetch and merge any commits from the tracking remote branch
git pull

# Merge just one specific commit from another branch to your current branch
git cherry-pick [commit_id]

# Fetches changes from the specified remote repository and merges them into the current branch.
git pull <remote>

# Fetches changes from the remote repository and rebases the current branch onto the updated branch.
git pull -rebase
