
## BELOW MENTIONED ARE THE GIT COMMANDS RELATED TO BRANCHING ##

# Commnad to lists all branches in the repository.
git branch

# Command to lists all local and remote branches.
git branch -a

# Command to switch to your specified branch 
git checkout branch_name

# Commadn to create a new branch and switches to it.
git checkout -b branch_name

# Command to delete the branch called my_branch
git branch -d branch_name

# Command to merge branchX into branchY
git checkout branchX
git merge branchY

# Command to tag the current commit
git tag my_tag
