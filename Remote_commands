## BELOW MENTIONED ARE GIT COMMANDS RELATED TO "REMOTE" ##

# Command to add a new remote repository with the specified name and URL.
git remote add [name] [url]

# Command to display the names of the remote repositories you've set up.
git remote

# Commmand to display the names and URLs of the remote repositories.
git remote -v

# Command to remove a remote repository.
git remote rm [remote repo name]

# ommand to change the URL of the git repo.
git remote set-url origin [git_url]
